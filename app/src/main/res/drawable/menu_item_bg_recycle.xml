<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/menu_item_bg_recycle"/>
        </shape>
    </item>
    <!-- SVG image as a bitmap -->
    <item android:bottom="30dp" android:drawable="@drawable/recycling_bags_package_svg"
        android:gravity="bottom|center_horizontal"
        android:height="140dp"
        android:width="140dp"
        />
</layer-list>